{
  "name": "ce-trainer-builder",
  "version": "1.0.0",
  "description": "CLI tool that wraps Cheat Engine table (.ct) files into standalone Windows trainer executables.",
  "type": "commonjs",
  "bin": {
    "trainer-builder": "./bin/trainer-builder.js"
  },
  "scripts": {
    "lint": "echo 'No lint step defined'",
    "test": "echo 'No tests defined'"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "pkg": "^5.8.1",
    "yargs": "^17.7.2"
  }
}
